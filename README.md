NIEM to Data Model Example
==================

An example Java Project to demonstrate the generation of a java package from NIEM XSD that can be used to generate instances 
from a domain model defined by Java classes. 
